import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    // paddingVertical: 24,
    paddingHorizontal: 20,
  },

  content: {
    paddingTop: 24,
    paddingBottom: 24,
  },
});